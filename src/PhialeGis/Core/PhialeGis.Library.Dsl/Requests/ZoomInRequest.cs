namespace PhialeGis.Library.Dsl.Requests
{
    /// <summary>
    /// Zoom in by a default step (runtime decides the factor).
    /// Grammar does not carry a numeric value here.
    /// </summary>
    public sealed class ZoomInRequest
    {
    }
}
