<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Controls.Core.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="SectionHeadlineStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Bahnschrift SemiBold" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="Foreground" Value="{DynamicResource DemoPrimaryTextBrush}" />
        <Setter Property="Margin" Value="0,0,0,14" />
    </Style>

    <Style x:Key="OverviewCardButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{DynamicResource DemoPanelBackgroundBrush}"
                            BorderBrush="{DynamicResource DemoPanelBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="14"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="8" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Background="{Binding AccentHex}" CornerRadius="14,0,0,14" />
                            <StackPanel Grid.Column="1" Margin="18,16,18,16">
                                <TextBlock Text="{Binding Title}"
                                           FontFamily="Bahnschrift SemiBold"
                                           FontSize="18"
                                           Foreground="{DynamicResource DemoPrimaryTextBrush}"
                                           Margin="0,0,0,10" />
                                <TextBlock Text="{Binding Description}"
                                           FontSize="13"
                                           Foreground="{DynamicResource DemoSecondaryTextBrush}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ToolbarButtonStyle" TargetType="Button" BasedOn="{StaticResource Button.Toolbar}" />

    <Style x:Key="ToolbarToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Margin" Value="0,0,10,10" />
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Background" Value="{DynamicResource DemoToolbarButtonBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoToolbarButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{DynamicResource DemoToolbarButtonTextBrush}" />
        <Setter Property="Opacity" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="ToolbarToggleBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ToolbarToggleBorder" Property="Background" Value="{DynamicResource DemoHintBackgroundBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ToolbarToggleBorder" Property="Background" Value="{DynamicResource DemoPlatformBadgeBackgroundBrush}" />
                            <Setter TargetName="ToolbarToggleBorder" Property="BorderBrush" Value="{DynamicResource DemoPlatformBadgeBorderBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.45" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CommitToolbarButtonStyle" TargetType="Button" BasedOn="{StaticResource Button.Toolbar.Primary}" />

    <Style x:Key="CancelToolbarButtonStyle" TargetType="Button" BasedOn="{StaticResource Button.Toolbar.Danger}" />

    <Style x:Key="ToolbarStatusTextStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{DynamicResource DemoSecondaryTextBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="ComponentCardButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="10" />
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="Background" Value="{DynamicResource DemoPanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DrawerToggleButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="40" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0,0,0,12" />
        <Setter Property="Background" Value="{DynamicResource DemoPanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource DemoHeaderTextBrush}" />
        <Setter Property="FontFamily" Value="Bahnschrift SemiBold" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="DrawerToggleBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="DrawerToggleBorder" Property="Background" Value="{DynamicResource DemoHintBackgroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DrawerGroupButtonStyle" TargetType="Button" BasedOn="{StaticResource ComponentCardButtonStyle}">
        <Setter Property="Padding" Value="14" />
        <Setter Property="Margin" Value="0,0,0,12" />
        <Style.Triggers>
            <Trigger Property="Tag" Value="True">
                <Setter Property="Background" Value="{DynamicResource DemoPlatformBadgeBackgroundBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource DemoPlatformBadgeBorderBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DrawerRailButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="44" />
        <Setter Property="Height" Value="44" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0,0,0,12" />
        <Setter Property="Background" Value="{DynamicResource DemoPanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="DrawerRailBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="DrawerRailBorder" Property="Background" Value="{DynamicResource DemoHintBackgroundBrush}" />
                        </Trigger>
                        <Trigger Property="Tag" Value="True">
                            <Setter TargetName="DrawerRailBorder" Property="Background" Value="{DynamicResource DemoPlatformBadgeBackgroundBrush}" />
                            <Setter TargetName="DrawerRailBorder" Property="BorderBrush" Value="{DynamicResource DemoPlatformBadgeBorderBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DemoTabControlStyle" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoTabContentBorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local"
                          Background="Transparent">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TabPanel Grid.Row="0"
                                  Panel.ZIndex="1"
                                  Margin="0,0,0,-1"
                                  Background="Transparent"
                                  IsItemsHost="True" />
                        <Border Grid.Row="1"
                                Background="{DynamicResource DemoTabContentBackgroundBrush}"
                                BorderBrush="{DynamicResource DemoTabContentBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="0,14,14,14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="SelectedContent" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DemoTabItemStyle" TargetType="TabItem">
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="Margin" Value="0,0,6,0" />
        <Setter Property="Background" Value="{DynamicResource DemoTabHeaderBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoTabHeaderBorderBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource DemoTabHeaderTextBrush}" />
        <Setter Property="FontFamily" Value="Bahnschrift SemiBold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="10,10,0,0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          ContentSource="Header" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource DemoTabHeaderSelectedBackgroundBrush}" />
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource DemoTabHeaderSelectedBorderBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource DemoPrimaryTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DemoStatusBannerBorderStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,0,12,10" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Background" Value="{DynamicResource DemoWarningBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoWarningBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
    </Style>

    <Style x:Key="DemoSurfaceBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource DemoPanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoPanelBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="12" />
    </Style>

    <Style x:Key="DemoCodeSurfaceBorderStyle" TargetType="Border" BasedOn="{StaticResource DemoSurfaceBorderStyle}">
        <Setter Property="Background" Value="{DynamicResource DemoCodeBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoCodeBorderBrush}" />
        <Setter Property="CornerRadius" Value="14" />
    </Style>

    <Style x:Key="DemoCodeTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource DemoCodeBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource DemoCodeTextBrush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource DemoCodeTextBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="18" />
        <Setter Property="FontFamily" Value="Consolas" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="AcceptsReturn" Value="True" />
        <Setter Property="AcceptsTab" Value="True" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="SelectionBrush" Value="#35506E" />
        <Setter Property="SelectionOpacity" Value="0.75" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="CodeTextBoxBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      Background="Transparent"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                                      VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="DemoTokenBorderStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,0,8,8" />
        <Setter Property="Padding" Value="8,3" />
        <Setter Property="Background" Value="{DynamicResource DemoTokenBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DemoTokenBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
    </Style>
</ResourceDictionary>
