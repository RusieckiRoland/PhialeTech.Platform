<UserControl x:Class="PhialeTech.PhialeGrid.Wpf.Surface.GridSurfaceHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PhialeTech.PhialeGrid.Wpf.Surface"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer x:Name="ScrollViewer"
                      HorizontalContentAlignment="Left"
                      VerticalContentAlignment="Top"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <local:GridSurfacePanel x:Name="SurfacePanel"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top" />
        </ScrollViewer>
        <Border x:Name="VerticalTrackMarkerHost"
                Width="10"
                Margin="0,0,2,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Stretch"
                Background="Transparent"
                IsHitTestVisible="False"
                Visibility="Collapsed"
                Panel.ZIndex="2">
            <Canvas x:Name="VerticalTrackMarkerCanvas"
                    Width="10"
                    Background="Transparent" />
        </Border>
    </Grid>
</UserControl>
